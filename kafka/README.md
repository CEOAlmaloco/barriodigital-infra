# kafka/ — ec2-kafka

Compose de Kafka + Zookeeper (Unidad 3).

- **EP1:** placeholder vacío a propósito.
- **U3:** cluster + topics para report/audit.

```powershell
docker compose -f compose.yml config
```
