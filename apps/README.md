# apps/ — ec2-apps

Compose de los microservicios BarrioDigital.

- **EP1:** solo el archivo placeholder (`services: {}`).
- Después: ir agregando servicios cuando cada repo tenga `Dockerfile`.

```powershell
docker compose -f compose.yml config
```
